<%	response.addCookie( new Cookie( "firstName", request.getParameter("firstName")));	response.addCookie( new Cookie( "lastName", request.getParameter("lastName")));	response.addCookie( new Cookie( "age", request.getParameter("age")));	response.addCookie( new Cookie( "city", request.getParameter("city")));%><html><body><h2>persist3.jsp receives all data in post requests and send it on to persist4.jspin 4 individual cookies.</h2>The request parameters received were:<br /><%	java.util.Enumeration list = request.getParameterNames();	while ( list.hasMoreElements()) {		String name = (String)list.nextElement();		out.print( name + " = <font color=\"red\">" + request.getParameter(name) + "</font><br />" );	}%><a href="persist4.jsp">Proceed to next page via link instead of a form.<br />Or just type in the address.<br />You don't <em>have</em> to have a link at all, since the cookies are now set.</a></body></html>